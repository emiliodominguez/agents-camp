node_modules
dist
*.local
.DS_Store

# Secrets and local harness configuration.
.env

# Runtime backend state — file-backed roster and chat transcripts
.agents/

# Raw asset downloads (only the extracted sheets in public/ are committed)
.assets-download

# Verification screenshots dropped at the repo root
/*.png
